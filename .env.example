# Copia este archivo a ".env" y pon tu clave de News API.
# Mientras no haya clave, la app usa datos simulados automáticamente.
# El archivo .env está ignorado por git (no se sube al repositorio).

VITE_NEWS_API_KEY=tu_clave_de_newsapi_org
